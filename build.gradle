group 'polyvolve'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

subprojects {
    tasks.withType(Jar) {
        destinationDir = file("$rootDir/out/server")
    }
}

allprojects {

}
